One arithmetic flag before push: forward_dates_confirmed (55) + window (51) + estimated (1315) = 1421, but tickers_with_forward_dates = 1370. A 51-entry gap means either some tickers carry more than one forward entry (in which case the three sub-counts are per-entry while the headline is per-ticker — fine, but the report should say so, e.g. rename to forward_entries_*), or 51 tickers are double-counted across precision classes, which would hint at a confirmed-vs-estimated collision the precision hierarchy should have resolved. Given AGL SJ, ambiguous metric semantics is exactly the failure mode this report exists to kill — worth a two-minute check of which explanation it is, and either a rename or a dedup, before the schema ossifies as pipeline-report/v1.

Also confirm one regression-rule nuance survived into the wiring: events_added_today == 0 should flag only when calendar.json expected reports that weekday — today's baseline correctly shows ok with zero adds, so it looks right, but the corruption test didn't exercise that particular rule; a quick mental trace of the condition is enough.