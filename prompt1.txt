Check-then-implement: the Summarize button + drivers feature. Parts of
this MAY already exist from prior sessions — verify each on disk before
building anything. State from disk first, per CLAUDE.md.

VERIFICATION PASS — report a table (component | exists? | evidence):
  a. data/summaries-schema.json: is there a v2 with a "drivers" array
     (metric, direction, explanation, basis: company-disclosed |
     derived-arithmetic)? Or v1 only?
  b. .claude/commands/earnings.md Step 2: does it extract drivers
     (causal-language grep: "due to", "driven by", "partially
     offset")? (Note: guidance extraction WAS added in 4fb41ee — that
     is a different subsection; check for drivers specifically.)
  c. SummaryPanel.tsx: does it render a "Why"/drivers section between
     summary_long and the footer?
  d. frontend/app/api/summarize/route.ts: does it exist, with Bearer
     CRON_SECRET gating, registry validation, 409-if-summary-exists,
     429-if-recent-dispatch, repository_dispatch type "summarize" via
     GH_PAT?
  e. Ticker-page UI: is there a Summarize button rendered where
     SummaryPanel would appear but no summary exists, with a pending
     state + 60s poll?
Print the table, then implement ONLY the missing rows:

[IF MISSING] PART 1 — SCHEMA v2: extend data/summaries-schema.json
($id summaries/v2, v1 files stay valid): "drivers": array of
{ metric (matches a kpi label or "overall"), direction: up|down|flat,
explanation (<=50 words, no verbatim quotes over 15 words),
basis: "company-disclosed" | "derived-arithmetic" }.
company-disclosed = the filing/MD&A explicitly states the cause;
derived-arithmetic = pure decomposition from our own numbers. HARD
RULE for the command: no macro narratives, no invented causes; if the
filing gives no reason, the driver is "not explained in the release".

[IF MISSING] PART 2 — COMMAND: earnings.md Step 2 gains a drivers
subsection: extract-doc-text.mjs --grep on causal language, map each
disclosed cause to its metric, mark basis. Miner rules extend here
(grades/throughput/recovery, realized vs benchmark price, by-product
credit swings, capitalized-vs-expensed stripping — when disclosed).

[IF MISSING] PART 3 — PANEL: "Why" section in SummaryPanel.tsx —
one line per driver: metric, direction arrow, explanation, subtle
basis tag (analysts must see company-stated vs computed).
"not explained in the release" renders muted.

[IF MISSING] PART 4 — BUTTON + ROUTE:
  a. /api/summarize route per spec in (d) above, reusing existing
     GH_PAT / GH_REPO_* / CRON_SECRET env vars.
  b. Summarize button on covered-tier ticker pages (data/covered.json
     gate) where no summary exists for the latest reported period;
     click → POST → "Summary in progress — ready in a few minutes"
     disabled state, 60s store poll swapping in SummaryPanel when the
     file lands.

TEST: whichever parts were built — run local /earnings on a covered
ticker WITHOUT an existing summary (check data/summaries/ first,
pick accordingly) to produce one v2 file with drivers; confirm any
existing v1 summaries still validate and render; button POST path
tested with curl (expect 202-style accept on a valid covered ticker,
409 on one with an existing summary, 401 without the bearer).
Standing tests green, push, report hash + the verification table +
which parts were newly built vs already present.